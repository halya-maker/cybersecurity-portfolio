# C++ Password Manager

A simple CLI password manager written in C++.

Features:
- Store passwords for websites
- Retrieve saved passwords
- Uses file storage

Compile:

g++ password_manager.cpp -o manager

Run:

./manager
